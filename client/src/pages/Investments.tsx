import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { investmentTemplateCsv, parseInvestmentCsv, type InvestmentImportRow } from "@/lib/investmentImport";
import { ChartNoAxesCombined, Download, Pencil, PiggyBank, Plus, Upload, WalletCards } from "lucide-react";
import { FormEvent, useMemo, useState } from "react";
import { toast } from "sonner";

const investmentTypes = [["savings", "Ahorro"], ["fixed_income", "Renta fija"], ["fund_etf", "Fondo o ETF"], ["stock", "Acciones"], ["crypto", "Criptoactivos"], ["land", "Terreno"], ["property", "Inmueble"], ["business_equity", "Participación empresarial"], ["retirement", "Retiro"], ["other", "Otro"]] as const;
const operationTypes = [["contribution", "Aportación"], ["withdrawal", "Retiro"], ["yield", "Rendimiento registrado"], ["valuation_adjustment", "Ajuste de valuación"]] as const;
const formatMoney = (value: number, currency: string) => new Intl.NumberFormat("es-MX", { style: "currency", currency, minimumFractionDigits: 2 }).format(value / 100);
const toDateValue = (date: Date | string | null | undefined) => date ? new Date(date).toISOString().slice(0, 10) : "";

export default function Investments() {
  const { data, isLoading } = trpc.finance.workspace.get.useQuery();
  const utils = trpc.useUtils();
  const [editing, setEditing] = useState<any | null>(null);
  const [open, setOpen] = useState(false);
  const [operationFor, setOperationFor] = useState<any | null>(null);
  const [importOpen, setImportOpen] = useState(false);
  const [importRows, setImportRows] = useState<InvestmentImportRow[]>([]);
  const [importErrors, setImportErrors] = useState<string[]>([]);
  const [importing, setImporting] = useState(false);
  const investments = (data?.investments ?? []) as any[];
  const operations = (data?.investmentOperations ?? []) as any[];
  const reportCurrency = data?.dashboard.reportCurrency ?? "MXN";
  const save = trpc.finance.workspace.investments.save.useMutation({ onSuccess: async () => { await utils.finance.workspace.get.invalidate(); setOpen(false); setEditing(null); toast.success("Posición guardada. La valuación sigue siendo manual."); }, onError: error => toast.error(error.message) });
  const operationSave = trpc.finance.workspace.investments.operationSave.useMutation({ onSuccess: async () => { await utils.finance.workspace.get.invalidate(); setOperationFor(null); toast.success("Operación registrada sin ejecutar ningún movimiento externo."); }, onError: error => toast.error(error.message) });
  const isOwner = data?.workspaceAccess.role === "owner";
  const includedValue = useMemo(() => investments.filter(item => item.includeInNetWorth && item.status !== "closed").reduce((sum, item) => sum + (item.currency === reportCurrency ? item.currentValueCents : item.reportCurrency === reportCurrency ? item.reportValueCents ?? 0 : 0), 0), [investments, reportCurrency]);
  const latestFor = (id: number) => operations.filter(item => item.investmentId === id).sort((a, b) => new Date(b.occurredAt).getTime() - new Date(a.occurredAt).getTime())[0];
  const downloadTemplate = () => { const url = URL.createObjectURL(new Blob([investmentTemplateCsv()], { type: "text/csv;charset=utf-8" })); const link = document.createElement("a"); link.href = url; link.download = "meximoney-inversiones-plantilla.csv"; link.click(); URL.revokeObjectURL(url); };
  const readImport = (file: File | undefined) => { if (!file) return; const reader = new FileReader(); reader.onload = () => { const result = parseInvestmentCsv(String(reader.result ?? "")); setImportRows(result.rows); setImportErrors(result.errors); }; reader.readAsText(file); };
  const confirmImport = async () => { if (importErrors.length || !importRows.length) return; setImporting(true); try { for (const row of importRows) await save.mutateAsync({ name: row.name, type: row.type as any, institution: row.institution || null, entityId: null, projectId: null, scope: "personal", currency: row.currency, costBasisCents: Math.round(row.cost * 100), currentValueCents: Math.round(row.current * 100), reportCurrency: row.currency === reportCurrency ? null : reportCurrency, reportValueCents: row.currency === reportCurrency ? null : null, exchangeRateMicros: null, exchangeRateDate: null, valuationDate: row.valuationDate ? new Date(`${row.valuationDate}T12:00:00`).getTime() : null, includeInNetWorth: row.includeInNetWorth, status: "active", notes: row.notes || null }); await utils.finance.workspace.get.invalidate(); setImportOpen(false); setImportRows([]); toast.success(`${importRows.length} posiciones importadas tras tu confirmación.`); } catch (error: any) { toast.error(error.message ?? "No se pudieron guardar las posiciones."); } finally { setImporting(false); } };

  return <section className="space-y-6">
    <header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">Activo manual y privado</p><h1 className="page-title">Ahorro e inversiones</h1><p className="page-subtitle">Registra posiciones, aportaciones y valuaciones confirmadas por ti. No hay cotizaciones, compra/venta ni conexión bancaria.</p></div>{isOwner ? <div className="flex flex-wrap gap-2"><Button variant="outline" onClick={() => setImportOpen(true)}><Upload className="mr-2 h-4 w-4"/>Importar CSV</Button><Button className="btn-primary" onClick={() => { setEditing(null); setOpen(true); }}><Plus className="mr-2 h-4 w-4"/>Nueva posición</Button></div> : null}</header>
    <div className="grid gap-3 sm:grid-cols-3"><Metric icon={PiggyBank} label="Valor incluido en patrimonio" value={formatMoney(includedValue, reportCurrency)} /><Metric icon={ChartNoAxesCombined} label="Posiciones activas" value={String(investments.filter(item => item.status === "active").length)} /><Metric icon={WalletCards} label="Moneda de reporte" value={reportCurrency} /></div>
    <Card className="surface-card"><CardContent className="p-4 text-sm text-muted-foreground"><strong className="text-foreground">Evita el doble conteo:</strong> registra aquí el activo económico —por ejemplo, un terreno, Bitcoin o la posición de ahorro— y usa una cuenta sólo para el efectivo disponible. Si el activo está en otra moneda, captura también su conversión manual antes de incluirlo en el patrimonio consolidado.</CardContent></Card>
    <div className="grid gap-4 lg:grid-cols-2">
      {isLoading ? <Card className="surface-card"><CardContent className="p-5 text-sm text-muted-foreground">Cargando posiciones…</CardContent></Card> : null}
      {!isLoading && investments.length === 0 ? <Card className="surface-card lg:col-span-2"><CardContent className="flex min-h-52 flex-col items-center justify-center gap-3 p-6 text-center"><PiggyBank className="h-8 w-8 text-muted-foreground"/><p className="font-medium">Aún no hay posiciones de ahorro o inversión</p><p className="max-w-lg text-sm text-muted-foreground">Puedes crear una cuenta de ahorro, terreno, acciones, Bitcoin, fondos u otros activos y anotar su valuación manual.</p>{isOwner ? <Button variant="outline" onClick={() => setOpen(true)}>Crear primera posición</Button> : null}</CardContent></Card> : null}
      {investments.map(item => <Card className="surface-card" key={item.id}><CardHeader className="pb-3"><div className="flex items-start justify-between gap-3"><div><CardTitle className="text-base">{item.name}</CardTitle><CardDescription>{investmentTypes.find(([value]) => value === item.type)?.[1] ?? "Otro"}{item.institution ? ` · ${item.institution}` : ""}</CardDescription></div><Badge variant={item.status === "active" ? "default" : "secondary"}>{item.status === "active" ? "Activa" : item.status === "paused" ? "En pausa" : "Cerrada"}</Badge></div></CardHeader><CardContent className="space-y-4"><div className="grid grid-cols-2 gap-3 text-sm"><div><p className="text-muted-foreground">Costo acumulado</p><strong>{formatMoney(item.costBasisCents, item.currency)}</strong></div><div><p className="text-muted-foreground">Valor manual</p><strong>{formatMoney(item.currentValueCents, item.currency)}</strong></div><div><p className="text-muted-foreground">Valuación</p><span>{item.valuationDate ? new Date(item.valuationDate).toLocaleDateString("es-MX") : "Pendiente"}</span></div><div><p className="text-muted-foreground">Patrimonio</p><span>{item.includeInNetWorth ? "Incluida" : "Excluida"}</span></div></div>{item.currency !== reportCurrency ? <p className="rounded-md bg-muted/60 p-2 text-xs text-muted-foreground">{item.reportCurrency === reportCurrency && item.reportValueCents != null ? `Valor reportado: ${formatMoney(item.reportValueCents, reportCurrency)}` : `Pendiente de conversión manual a ${reportCurrency}.`}</p> : null}{latestFor(item.id) ? <p className="text-xs text-muted-foreground">Última operación: {operationTypes.find(([value]) => value === latestFor(item.id).type)?.[1]} · {new Date(latestFor(item.id).occurredAt).toLocaleDateString("es-MX")}</p> : null}{isOwner ? <div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={() => { setEditing(item); setOpen(true); }}><Pencil className="mr-1.5 h-3.5 w-3.5"/>Editar</Button><Button size="sm" variant="ghost" onClick={() => setOperationFor(item)}><Plus className="mr-1.5 h-3.5 w-3.5"/>Operación</Button></div> : null}</CardContent></Card>)}
    </div>
    <Dialog open={open} onOpenChange={value => { setOpen(value); if (!value) setEditing(null); }}><InvestmentForm investment={editing} entities={data?.entities ?? []} projects={data?.projects ?? []} reportCurrency={reportCurrency} saving={save.isPending} onSubmit={(input: any) => save.mutate(input)} /></Dialog>
    <Dialog open={!!operationFor} onOpenChange={value => { if (!value) setOperationFor(null); }}>{operationFor ? <OperationForm investment={operationFor} transactions={data?.transactions ?? []} saving={operationSave.isPending} onSubmit={(input: any) => operationSave.mutate(input)} /> : null}</Dialog>
    <Dialog open={importOpen} onOpenChange={value => { setImportOpen(value); if (!value) { setImportRows([]); setImportErrors([]); } }}><DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>Importar posiciones desde CSV</DialogTitle><DialogDescription>El archivo se lee en este navegador, no se conserva y sólo se guardará al confirmar la vista previa.</DialogDescription></DialogHeader><div className="space-y-4 py-2"><div className="flex flex-wrap gap-2"><Button type="button" variant="outline" onClick={downloadTemplate}><Download className="mr-2 h-4 w-4"/>Descargar plantilla</Button><Input type="file" accept=".csv,text/csv" onChange={event => readImport(event.target.files?.[0])}/></div><p className="text-xs text-muted-foreground">Columnas: Nombre, Tipo, Institucion, Moneda, Costo, Valor_actual, Fecha_valoracion, Incluir_patrimonio y Notas. Tipos permitidos: savings, fixed_income, fund_etf, stock, crypto, land, property, business_equity, retirement u other.</p>{importErrors.length ? <div className="rounded-md border border-destructive/40 p-3 text-sm text-destructive">{importErrors.map(error => <p key={error}>{error}</p>)}</div> : null}{importRows.length && !importErrors.length ? <div className="space-y-2"><p className="text-sm font-medium">Vista previa: {importRows.length} posiciones</p><div className="max-h-56 overflow-auto rounded-md border"><table className="w-full text-sm"><thead><tr className="bg-muted/50 text-left"><th className="p-2">Nombre</th><th className="p-2">Tipo</th><th className="p-2">Valor</th></tr></thead><tbody>{importRows.map((row, index) => <tr key={`${row.name}-${index}`} className="border-t"><td className="p-2">{row.name}</td><td className="p-2">{row.type}</td><td className="p-2">{row.current} {row.currency}</td></tr>)}</tbody></table></div><Button className="btn-primary" disabled={importing} onClick={confirmImport}>{importing ? "Guardando…" : "Confirmar e importar"}</Button></div> : null}</div></DialogContent></Dialog>
  </section>;
}

function Metric({ icon: Icon, label, value }: { icon: any; label: string; value: string }) { return <Card className="surface-card"><CardContent className="flex items-center gap-3 p-4"><span className="rounded-xl bg-primary/10 p-2 text-primary"><Icon className="h-4 w-4"/></span><div><p className="text-xs text-muted-foreground">{label}</p><p className="font-semibold">{value}</p></div></CardContent></Card>; }

function InvestmentForm({ investment, entities, projects, reportCurrency, saving, onSubmit }: any) {
  const initial = investment ? { ...investment, entityId: investment.entityId?.toString() ?? "none", projectId: investment.projectId?.toString() ?? "none", institution: investment.institution ?? "", reportCurrency: investment.reportCurrency ?? reportCurrency, reportValue: investment.reportValueCents != null ? String(investment.reportValueCents / 100) : "", cost: String(investment.costBasisCents / 100), current: String(investment.currentValueCents / 100), exchangeRate: investment.exchangeRateMicros ? String(investment.exchangeRateMicros / 1_000_000) : "", valuationDate: toDateValue(investment.valuationDate), notes: investment.notes ?? "" } : { name: "", type: "savings", entityId: "none", projectId: "none", institution: "", scope: "personal", currency: reportCurrency, cost: "0", current: "0", reportCurrency, reportValue: "", exchangeRate: "", valuationDate: toDateValue(new Date()), includeInNetWorth: true, status: "active", notes: "" };
  const [form, setForm] = useState(initial);
  const update = (key: string, value: any) => setForm((current: any) => ({ ...current, [key]: value }));
  const submit = (event: FormEvent) => { event.preventDefault(); const cents = (value: string) => Math.round((Number(value) || 0) * 100); onSubmit({ id: investment?.id, name: form.name, type: form.type, institution: form.institution || null, entityId: form.entityId === "none" ? null : Number(form.entityId), projectId: form.projectId === "none" ? null : Number(form.projectId), scope: form.scope, currency: form.currency.toUpperCase(), costBasisCents: cents(form.cost), currentValueCents: cents(form.current), reportCurrency: form.currency === reportCurrency ? null : form.reportCurrency.toUpperCase(), reportValueCents: form.currency === reportCurrency ? null : form.reportValue === "" ? null : cents(form.reportValue), exchangeRateMicros: form.exchangeRate ? Math.round(Number(form.exchangeRate) * 1_000_000) : null, exchangeRateDate: form.valuationDate ? new Date(`${form.valuationDate}T12:00:00`).getTime() : null, valuationDate: form.valuationDate ? new Date(`${form.valuationDate}T12:00:00`).getTime() : null, includeInNetWorth: form.includeInNetWorth, status: form.status, notes: form.notes || null }); };
  return <DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>{investment ? "Editar posición" : "Nueva posición"}</DialogTitle><DialogDescription>La valuación es manual y no genera órdenes, transferencias ni precios de mercado.</DialogDescription></DialogHeader><form onSubmit={submit} className="grid gap-4 py-2"><div className="grid gap-2"><Label>Nombre de la posición</Label><Input required value={form.name} onChange={event => update("name", event.target.value)} placeholder="Ej. Fondo de emergencia o Bitcoin"/></div><div className="grid gap-4 sm:grid-cols-2"><FieldSelect label="Tipo" value={form.type} onChange={(value: string) => update("type", value)} options={investmentTypes}/><div className="grid gap-2"><Label>Institución, plataforma o emisor</Label><Input value={form.institution} onChange={event => update("institution", event.target.value)} placeholder="Opcional"/></div></div><div className="grid gap-4 sm:grid-cols-3"><FieldSelect label="Entidad" value={form.entityId} onChange={(value: string) => update("entityId", value)} options={[["none", "Sin entidad"], ...entities.map((item: any) => [String(item.id), item.name]) ]}/><FieldSelect label="Proyecto" value={form.projectId} onChange={(value: string) => update("projectId", value)} options={[["none", "Sin proyecto"], ...projects.filter((item: any) => form.entityId === "none" || item.entityId === Number(form.entityId)).map((item: any) => [String(item.id), item.name]) ]}/><FieldSelect label="Ámbito" value={form.scope} onChange={(value: string) => update("scope", value)} options={[["personal", "Personal"], ["business", "Empresa"], ["mixed", "Mixto"]]}/></div><div className="grid gap-4 sm:grid-cols-3"><div className="grid gap-2"><Label>Moneda</Label><Input required maxLength={3} value={form.currency} onChange={event => update("currency", event.target.value.toUpperCase())}/></div><MoneyInput label="Costo acumulado" value={form.cost} onChange={(value: string) => update("cost", value)}/><MoneyInput label="Valor manual actual" value={form.current} onChange={(value: string) => update("current", value)}/></div>{form.currency !== reportCurrency ? <div className="rounded-lg border p-3"><p className="mb-3 text-sm font-medium">Conversión manual a {reportCurrency}</p><div className="grid gap-3 sm:grid-cols-2"><MoneyInput label={`Valor reportado (${reportCurrency})`} value={form.reportValue} onChange={(value: string) => update("reportValue", value)}/><div className="grid gap-2"><Label>Tipo de cambio usado</Label><Input inputMode="decimal" value={form.exchangeRate} onChange={event => update("exchangeRate", event.target.value)}/></div></div></div> : null}<div className="grid gap-4 sm:grid-cols-3"><div className="grid gap-2"><Label>Fecha de valuación</Label><Input type="date" value={form.valuationDate} onChange={event => update("valuationDate", event.target.value)}/></div><FieldSelect label="Estado" value={form.status} onChange={(value: string) => update("status", value)} options={[["active", "Activa"], ["paused", "En pausa"], ["closed", "Cerrada"]]}/><label className="flex items-center gap-2 pt-7 text-sm"><Checkbox checked={form.includeInNetWorth} onCheckedChange={value => update("includeInNetWorth", value === true)}/>Incluir en patrimonio</label></div><div className="grid gap-2"><Label>Notas privadas</Label><Textarea value={form.notes} onChange={event => update("notes", event.target.value)} maxLength={3000}/></div><Button type="submit" className="btn-primary" disabled={saving}>{saving ? "Guardando…" : "Guardar posición"}</Button></form></DialogContent>;
}

function OperationForm({ investment, transactions, saving, onSubmit }: any) { const [form, setForm] = useState({ type: "contribution", amount: "", occurredAt: toDateValue(new Date()), linkedTransactionId: "none", notes: "" }); const submit = (event: FormEvent) => { event.preventDefault(); onSubmit({ investmentId: investment.id, type: form.type, amountCents: Math.round((Number(form.amount) || 0) * 100), currency: investment.currency, occurredAt: new Date(`${form.occurredAt}T12:00:00`).getTime(), linkedTransactionId: form.linkedTransactionId === "none" ? null : Number(form.linkedTransactionId), notes: form.notes || null }); }; return <DialogContent className="sm:max-w-xl"><DialogHeader><DialogTitle>Registrar operación</DialogTitle><DialogDescription>La operación deja historial. Actualiza la valuación de la posición por separado cuando corresponda.</DialogDescription></DialogHeader><form onSubmit={submit} className="grid gap-4 py-2"><FieldSelect label="Tipo" value={form.type} onChange={(value: string) => setForm({ ...form, type: value })} options={operationTypes}/><MoneyInput label={`Importe (${investment.currency})`} value={form.amount} onChange={(value: string) => setForm({ ...form, amount: value })}/><div className="grid gap-2"><Label>Fecha</Label><Input required type="date" value={form.occurredAt} onChange={event => setForm({ ...form, occurredAt: event.target.value })}/></div><FieldSelect label="Movimiento manual ya registrado (opcional)" value={form.linkedTransactionId} onChange={(value: string) => setForm({ ...form, linkedTransactionId: value })} options={[["none", "Sin enlace"], ...transactions.filter((item: any) => item.currency === investment.currency).map((item: any) => [String(item.id), `${new Date(item.occurredAt).toLocaleDateString("es-MX")} · ${formatMoney(item.amountCents, item.currency)} · ${item.notes || item.type}`]) ]}/><div className="grid gap-2"><Label>Notas</Label><Textarea value={form.notes} onChange={event => setForm({ ...form, notes: event.target.value })}/></div><Button type="submit" className="btn-primary" disabled={saving || !form.amount || Number(form.amount) <= 0}>{saving ? "Guardando…" : "Guardar operación"}</Button></form></DialogContent>; }

function FieldSelect({ label, value, onChange, options }: { label: string; value: string; onChange: (value: string) => void; options: readonly (readonly string[])[] }) { return <div className="grid gap-2"><Label>{label}</Label><Select value={value} onValueChange={onChange}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{options.map(([optionValue, optionLabel]) => <SelectItem key={optionValue} value={optionValue}>{optionLabel}</SelectItem>)}</SelectContent></Select></div>; }
function MoneyInput({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <div className="grid gap-2"><Label>{label}</Label><Input required inputMode="decimal" min="0" step="0.01" value={value} onChange={event => onChange(event.target.value)}/></div>; }
