import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Checkbox } from "@/components/ui/checkbox";
import { Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { buildInvestmentExportRows, exportInvestmentHistory } from "@/lib/investmentExport";
import { investmentTemplateCsv, parseInvestmentCsv, type InvestmentImportRow } from "@/lib/investmentImport";
import { Car, ChartNoAxesCombined, Download, House, Pencil, PiggyBank, Plus, Trash2, Upload, WalletCards } from "lucide-react";
import { FormEvent, useMemo, useState } from "react";
import { toast } from "sonner";

const investmentTypes = [["savings", "Ahorro"], ["fixed_income", "Renta fija"], ["fund_etf", "Fondo o ETF"], ["stock", "Acciones"], ["crypto", "Criptoactivos"], ["land", "Terreno"], ["property", "Inmueble"], ["vehicle", "Vehículo"], ["business_equity", "Participación empresarial"], ["retirement", "Retiro"], ["other", "Otro"]] as const;
const operationTypes = [["contribution", "Aportación"], ["withdrawal", "Retiro"], ["yield", "Rendimiento registrado"], ["valuation_adjustment", "Apreciación o ajuste al alza"], ["depreciation", "Depreciación o ajuste a la baja"]] as const;
const formatMoney = (value: number, currency: string) => new Intl.NumberFormat("es-MX", { style: "currency", currency, minimumFractionDigits: 2 }).format(value / 100);
const toDateValue = (date: Date | string | null | undefined) => date ? new Date(date).toISOString().slice(0, 10) : "";

export default function Investments() {
  const { data, isLoading } = trpc.finance.workspace.get.useQuery();
  const utils = trpc.useUtils();
  const [editing, setEditing] = useState<any | null>(null);
  const [open, setOpen] = useState(false);
  const [operationFor, setOperationFor] = useState<any | null>(null);
  const [financedOpen, setFinancedOpen] = useState(false);
  const [importOpen, setImportOpen] = useState(false);
  const [importRows, setImportRows] = useState<InvestmentImportRow[]>([]);
  const [importErrors, setImportErrors] = useState<string[]>([]);
  const [importing, setImporting] = useState(false);
  const investments = (data?.investments ?? []) as any[];
  const operations = (data?.investmentOperations ?? []) as any[];
  const { data: financedAssets = [] } = trpc.finance.workspace.financedAssets.list.useQuery(undefined, { enabled: Boolean(data) });
  const reportCurrency = data?.dashboard.reportCurrency ?? "MXN";
  const refreshInvestments = async () => { await Promise.all([utils.finance.workspace.get.invalidate(), utils.finance.dashboard.invalidate()]); await utils.finance.workspace.get.refetch(); };
  const save = trpc.finance.workspace.investments.save.useMutation({ onSuccess: async () => { await refreshInvestments(); setOpen(false); setEditing(null); toast.success("Posición guardada y patrimonio actualizado. La valuación sigue siendo manual."); }, onError: error => toast.error(error.message) });
  const operationSave = trpc.finance.workspace.investments.operationSave.useMutation({ onSuccess: async () => { await refreshInvestments(); setOperationFor(null); toast.success("Operación registrada y posición actualizada, sin ejecutar ningún movimiento externo."); }, onError: error => toast.error(error.message) });
  const createFinancedAsset = trpc.finance.workspace.financedAssets.create.useMutation({ onSuccess: async () => { await refreshInvestments(); setFinancedOpen(false); toast.success("Activo, anticipo trazable y financiación registrados. No se ejecutó ningún pago externo."); }, onError: error => toast.error(error.message) });
  const operationRemove = trpc.finance.workspace.investments.operationRemove.useMutation({ onSuccess: async () => { await refreshInvestments(); toast.success("Operación eliminada y posición recalculada."); }, onError: error => toast.error(error.message) });
  const reconcileInvestment = trpc.finance.workspace.investments.reconcile.useMutation({ onSuccess: async result => { await refreshInvestments(); toast.success(`Posición reconciliada: ${formatMoney(result.currentValueCents, reportCurrency)} según su historial.`); }, onError: error => toast.error(error.message) });
  const isOwner = data?.workspaceAccess.role === "owner";
  const includedValue = useMemo(() => investments.filter(item => item.includeInNetWorth && item.status !== "closed").reduce((sum, item) => sum + (item.currency === reportCurrency ? item.currentValueCents : item.reportCurrency === reportCurrency ? item.reportValueCents ?? 0 : 0), 0), [investments, reportCurrency]);
  const latestFor = (id: number) => operations.filter(item => item.investmentId === id).sort((a, b) => new Date(b.occurredAt).getTime() - new Date(a.occurredAt).getTime())[0];
  const downloadTemplate = () => { const url = URL.createObjectURL(new Blob([investmentTemplateCsv()], { type: "text/csv;charset=utf-8" })); const link = document.createElement("a"); link.href = url; link.download = "meximoney-inversiones-plantilla.csv"; link.click(); URL.revokeObjectURL(url); };
  const readImport = (file: File | undefined) => { if (!file) return; const reader = new FileReader(); reader.onload = () => { const result = parseInvestmentCsv(String(reader.result ?? "")); setImportRows(result.rows); setImportErrors(result.errors); }; reader.readAsText(file); };
  const confirmImport = async () => { if (importErrors.length || !importRows.length) return; setImporting(true); try { for (const row of importRows) await save.mutateAsync({ name: row.name, type: row.type as any, institution: row.institution || null, entityId: null, projectId: null, scope: "personal", currency: row.currency, costBasisCents: Math.round(row.cost * 100), currentValueCents: Math.round(row.current * 100), reportCurrency: row.currency === reportCurrency ? null : reportCurrency, reportValueCents: row.currency === reportCurrency ? null : null, exchangeRateMicros: null, exchangeRateDate: null, valuationDate: row.valuationDate ? new Date(`${row.valuationDate}T12:00:00`).getTime() : null, includeInNetWorth: row.includeInNetWorth, status: "active", notes: row.notes || null }); await refreshInvestments(); setImportOpen(false); setImportRows([]); toast.success(`${importRows.length} posiciones importadas y patrimonio actualizado.`); } catch (error: any) { toast.error(error.message ?? "No se pudieron guardar las posiciones."); } finally { setImporting(false); } };
  const exportRows = useMemo(() => buildInvestmentExportRows(investments, operations), [investments, operations]);

  return <section className="space-y-6">
    <header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">Activo manual y privado</p><h1 className="page-title">Ahorro e inversiones</h1><p className="page-subtitle">Registra posiciones, aportaciones y valuaciones confirmadas por ti. No hay cotizaciones, compra/venta ni conexión bancaria.</p></div>{isOwner ? <div className="flex flex-wrap gap-2"><Button variant="outline" disabled={!exportRows.length} onClick={() => exportInvestmentHistory("csv", exportRows)}><Download className="mr-2 h-4 w-4"/>CSV</Button><Button variant="outline" disabled={!exportRows.length} onClick={() => exportInvestmentHistory("xlsx", exportRows)}>Excel</Button><Button variant="outline" disabled={!exportRows.length} onClick={() => exportInvestmentHistory("pdf", exportRows)}>PDF</Button><Button variant="outline" onClick={() => setImportOpen(true)}><Upload className="mr-2 h-4 w-4"/>Importar CSV</Button><Button variant="outline" onClick={() => setFinancedOpen(true)}><Car className="mr-2 h-4 w-4"/>Activo financiado</Button><Button className="btn-primary" onClick={() => { setEditing(null); setOpen(true); }}><Plus className="mr-2 h-4 w-4"/>Nueva posición</Button></div> : null}</header>
    <div className="grid gap-3 sm:grid-cols-3"><Metric icon={PiggyBank} label="Valor incluido en patrimonio" value={formatMoney(includedValue, reportCurrency)} /><Metric icon={ChartNoAxesCombined} label="Posiciones activas" value={String(investments.filter(item => item.status === "active").length)} /><Metric icon={WalletCards} label="Moneda de reporte" value={reportCurrency} /></div>
    <Card className="surface-card"><CardContent className="p-4 text-sm text-muted-foreground"><strong className="text-foreground">Evita el doble conteo:</strong> registra aquí el activo económico —por ejemplo, un terreno, Bitcoin o la posición de ahorro— y usa una cuenta sólo para el efectivo disponible. Si el activo está en otra moneda, captura también su conversión manual antes de incluirlo en el patrimonio consolidado.</CardContent></Card>
    {financedAssets.length ? <Card className="surface-card"><CardHeader className="pb-3"><CardTitle className="text-base">Activos financiados</CardTitle><CardDescription>El valor del activo y el saldo pendiente se muestran por separado en Patrimonio.</CardDescription></CardHeader><CardContent className="grid gap-3 md:grid-cols-2">{financedAssets.map((item: any) => { const asset = item.asset; const debt = item.debt; const equity = (asset?.currentValueCents ?? 0) - (debt?.balanceCents ?? 0); return <div className="rounded-xl border border-border/70 p-3" key={item.id}><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{asset?.name ?? "Activo vinculado"}</p><p className="text-xs text-muted-foreground">{item.valuationPolicy === "depreciating" ? "Depreciación manual" : item.valuationPolicy === "appreciating" ? "Apreciación manual" : "Valuación manual"}</p></div>{asset?.type === "property" ? <House className="size-4 text-primary"/> : <Car className="size-4 text-primary"/>}</div><div className="mt-3 grid grid-cols-3 gap-2 text-xs"><div><p className="text-muted-foreground">Valor</p><strong>{asset ? formatMoney(asset.currentValueCents, asset.currency) : "—"}</strong></div><div><p className="text-muted-foreground">Deuda</p><strong>{debt ? formatMoney(debt.balanceCents, debt.currency) : "—"}</strong></div><div><p className="text-muted-foreground">Neto</p><strong>{asset ? formatMoney(equity, asset.currency) : "—"}</strong></div></div></div>; })}</CardContent></Card> : null}
    <div className="grid gap-4 lg:grid-cols-2">
      {isLoading ? <Card className="surface-card"><CardContent className="p-5 text-sm text-muted-foreground">Cargando posiciones…</CardContent></Card> : null}
      {!isLoading && investments.length === 0 ? <Card className="surface-card lg:col-span-2"><CardContent className="flex min-h-52 flex-col items-center justify-center gap-3 p-6 text-center"><PiggyBank className="h-8 w-8 text-muted-foreground"/><p className="font-medium">Aún no hay posiciones de ahorro o inversión</p><p className="max-w-lg text-sm text-muted-foreground">Puedes crear una cuenta de ahorro, terreno, acciones, Bitcoin, fondos u otros activos y anotar su valuación manual.</p>{isOwner ? <Button variant="outline" onClick={() => setOpen(true)}>Crear primera posición</Button> : null}</CardContent></Card> : null}
      {investments.map(item => { const itemOperations = operations.filter(operation => operation.investmentId === item.id).sort((a, b) => new Date(b.occurredAt).getTime() - new Date(a.occurredAt).getTime()); const linkedGoal = data?.goals.find((goal: any) => goal.id === item.goalId); const historyTotals = itemOperations.reduce((total, operation) => operation.type === "contribution" ? { cost: total.cost + operation.amountCents, value: total.value + operation.amountCents } : operation.type === "withdrawal" ? { cost: Math.max(0, total.cost - operation.amountCents), value: Math.max(0, total.value - operation.amountCents) } : operation.type === "depreciation" ? { cost: total.cost, value: Math.max(0, total.value - operation.amountCents) } : { cost: total.cost, value: total.value + operation.amountCents }, { cost: 0, value: 0 }); const needsReconciliation = item.costBasisCents !== historyTotals.cost || item.currentValueCents !== historyTotals.value; return <Card className="surface-card" key={item.id}><CardHeader className="pb-3"><div className="flex items-start justify-between gap-3"><div><CardTitle className="text-base">{item.name}</CardTitle><CardDescription>{investmentTypes.find(([value]) => value === item.type)?.[1] ?? "Otro"}{item.institution ? ` · ${item.institution}` : ""}</CardDescription></div><Badge variant={item.status === "active" ? "default" : "secondary"}>{item.status === "active" ? "Activa" : item.status === "paused" ? "En pausa" : "Cerrada"}</Badge></div></CardHeader><CardContent className="space-y-4"><div className="grid grid-cols-2 gap-3 text-sm"><div><p className="text-muted-foreground">Costo acumulado</p><strong>{formatMoney(item.costBasisCents, item.currency)}</strong></div><div><p className="text-muted-foreground">Valor manual</p><strong>{formatMoney(item.currentValueCents, item.currency)}</strong></div><div><p className="text-muted-foreground">Valuación</p><span>{item.valuationDate ? new Date(item.valuationDate).toLocaleDateString("es-MX") : "Pendiente"}</span></div><div><p className="text-muted-foreground">Patrimonio</p><span>{item.includeInNetWorth ? "Incluida" : "Excluida"}</span></div></div>{linkedGoal ? <p className="rounded-md bg-primary/5 p-2 text-xs text-muted-foreground"><strong className="text-foreground">Objetivo vinculado:</strong> <a className="font-semibold text-primary underline-offset-4 hover:underline" href="/planificacion">{linkedGoal.name}</a>. Su valor sigue siendo manual y se muestra una sola vez en patrimonio.</p> : null}{item.currency !== reportCurrency ? <p className="rounded-md bg-muted/60 p-2 text-xs text-muted-foreground">{item.reportCurrency === reportCurrency && item.reportValueCents != null ? `Valor reportado: ${formatMoney(item.reportValueCents, reportCurrency)}` : `Pendiente de conversión manual a ${reportCurrency}.`}</p> : null}{needsReconciliation ? <div className="rounded-lg border border-amber-300 bg-amber-50/50 p-3 text-sm dark:bg-amber-950/20"><p className="font-medium">El historial no coincide con el valor de esta posición</p><p className="mt-1 text-muted-foreground">Revisa cada aporte. Tras eliminar cualquier duplicado, puedes recalcular costo y valor desde el historial guardado.</p>{isOwner ? <Button size="sm" variant="outline" className="mt-3" disabled={reconcileInvestment.isPending} onClick={() => { if (window.confirm("Se reemplazará el costo y valor manual por el total de las operaciones mostradas. Revisa primero los duplicados.")) reconcileInvestment.mutate({ investmentId: item.id }); }}>{reconcileInvestment.isPending ? "Recalculando…" : "Recalcular desde historial"}</Button> : null}</div> : null}{itemOperations.length ? <div className="rounded-lg border border-border/70"><div className="flex items-center justify-between border-b border-border/70 px-3 py-2"><p className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Historial de operaciones</p><span className="text-xs text-muted-foreground">{itemOperations.length} registro{itemOperations.length === 1 ? "" : "s"}</span></div><div className="divide-y divide-border/70">{itemOperations.map(operation => <div className="flex items-center justify-between gap-3 px-3 py-2 text-sm" key={operation.id}><div className="min-w-0"><p className="font-medium">{operationTypes.find(([value]) => value === operation.type)?.[1] ?? operation.type} · {formatMoney(operation.amountCents, operation.currency)}</p><p className="truncate text-xs text-muted-foreground">{new Date(operation.occurredAt).toLocaleDateString("es-MX")}{operation.linkedTransactionId ? " · Movimiento vinculado" : ""}{operation.notes ? ` · ${operation.notes}` : ""}</p></div>{isOwner ? <Button size="icon" variant="ghost" className="shrink-0 text-muted-foreground hover:text-destructive" aria-label={`Eliminar operación de ${item.name}`} disabled={operationRemove.isPending} onClick={() => { if (window.confirm("¿Eliminar esta operación? El costo y valor de la posición se recalcularán.")) operationRemove.mutate({ id: operation.id }); }}><Trash2 className="h-4 w-4"/></Button> : null}</div>)}</div></div> : null}{isOwner ? <div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={() => { setEditing(item); setOpen(true); }}><Pencil className="mr-1.5 h-3.5 w-3.5"/>Editar</Button><Button size="sm" variant="ghost" onClick={() => setOperationFor(item)}><Plus className="mr-1.5 h-3.5 w-3.5"/>Operación</Button></div> : null}</CardContent></Card>; })}
    </div>
    <Dialog open={open} onOpenChange={value => { setOpen(value); if (!value) setEditing(null); }}><InvestmentForm investment={editing} entities={data?.entities ?? []} projects={data?.projects ?? []} goals={data?.goals ?? []} reportCurrency={reportCurrency} saving={save.isPending} onSubmit={(input: any) => save.mutate(input)} /></Dialog>
    <Dialog open={financedOpen} onOpenChange={setFinancedOpen}><FinancedAssetForm accounts={data?.accounts ?? []} entities={data?.entities ?? []} projects={data?.projects ?? []} reportCurrency={reportCurrency} saving={createFinancedAsset.isPending} onSubmit={(input: any) => createFinancedAsset.mutate(input)} /></Dialog>
    <Dialog open={!!operationFor} onOpenChange={value => { if (!value) setOperationFor(null); }}>{operationFor ? <OperationForm investment={operationFor} transactions={data?.transactions ?? []} saving={operationSave.isPending} onSubmit={(input: any) => operationSave.mutate(input)} /> : null}</Dialog>
    <Dialog open={importOpen} onOpenChange={value => { setImportOpen(value); if (!value) { setImportRows([]); setImportErrors([]); } }}><DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>Importar posiciones desde CSV</DialogTitle><DialogDescription>El archivo se lee en este navegador, no se conserva y sólo se guardará al confirmar la vista previa.</DialogDescription></DialogHeader><div className="space-y-4 py-2"><div className="flex flex-wrap gap-2"><Button type="button" variant="outline" onClick={downloadTemplate}><Download className="mr-2 h-4 w-4"/>Descargar plantilla</Button><Input type="file" accept=".csv,text/csv" onChange={event => readImport(event.target.files?.[0])}/></div><p className="text-xs text-muted-foreground">Columnas: Nombre, Tipo, Institucion, Moneda, Costo, Valor_actual, Fecha_valoracion, Incluir_patrimonio y Notas. Tipos permitidos: savings, fixed_income, fund_etf, stock, crypto, land, property, business_equity, retirement u other.</p>{importErrors.length ? <div className="rounded-md border border-destructive/40 p-3 text-sm text-destructive">{importErrors.map(error => <p key={error}>{error}</p>)}</div> : null}{importRows.length && !importErrors.length ? <div className="space-y-2"><p className="text-sm font-medium">Vista previa: {importRows.length} posiciones</p><div className="max-h-56 overflow-auto rounded-md border"><table className="w-full text-sm"><thead><tr className="bg-muted/50 text-left"><th className="p-2">Nombre</th><th className="p-2">Tipo</th><th className="p-2">Valor</th></tr></thead><tbody>{importRows.map((row, index) => <tr key={`${row.name}-${index}`} className="border-t"><td className="p-2">{row.name}</td><td className="p-2">{row.type}</td><td className="p-2">{row.current} {row.currency}</td></tr>)}</tbody></table></div><Button className="btn-primary" disabled={importing} onClick={confirmImport}>{importing ? "Guardando…" : "Confirmar e importar"}</Button></div> : null}</div></DialogContent></Dialog>
  </section>;
}

function Metric({ icon: Icon, label, value }: { icon: any; label: string; value: string }) { return <Card className="surface-card"><CardContent className="flex items-center gap-3 p-4"><span className="rounded-xl bg-primary/10 p-2 text-primary"><Icon className="h-4 w-4"/></span><div><p className="text-xs text-muted-foreground">{label}</p><p className="font-semibold">{value}</p></div></CardContent></Card>; }

function InvestmentForm({ investment, entities, projects, goals, reportCurrency, saving, onSubmit }: any) {
  const initial = investment ? { ...investment, entityId: investment.entityId?.toString() ?? "none", projectId: investment.projectId?.toString() ?? "none", goalId: investment.goalId?.toString() ?? "none", institution: investment.institution ?? "", reportCurrency: investment.reportCurrency ?? reportCurrency, reportValue: investment.reportValueCents != null ? String(investment.reportValueCents / 100) : "", cost: String(investment.costBasisCents / 100), current: String(investment.currentValueCents / 100), exchangeRate: investment.exchangeRateMicros ? String(investment.exchangeRateMicros / 1_000_000) : "", valuationDate: toDateValue(investment.valuationDate), notes: investment.notes ?? "" } : { name: "", type: "savings", entityId: "none", projectId: "none", goalId: "none", institution: "", scope: "personal", currency: reportCurrency, cost: "0", current: "0", reportCurrency, reportValue: "", exchangeRate: "", valuationDate: toDateValue(new Date()), includeInNetWorth: true, status: "active", notes: "" };
  const [form, setForm] = useState(initial);
  const update = (key: string, value: any) => setForm((current: any) => ({ ...current, [key]: value }));
  const submit = (event: FormEvent) => { event.preventDefault(); const cents = (value: string) => Math.round((Number(value) || 0) * 100); onSubmit({ id: investment?.id, name: form.name, type: form.type, institution: form.institution || null, entityId: form.entityId === "none" ? null : Number(form.entityId), projectId: form.projectId === "none" ? null : Number(form.projectId), goalId: form.goalId === "none" ? null : Number(form.goalId), scope: form.scope, currency: form.currency.toUpperCase(), costBasisCents: cents(form.cost), currentValueCents: cents(form.current), reportCurrency: form.currency === reportCurrency ? null : form.reportCurrency.toUpperCase(), reportValueCents: form.currency === reportCurrency ? null : form.reportValue === "" ? null : cents(form.reportValue), exchangeRateMicros: form.exchangeRate ? Math.round(Number(form.exchangeRate) * 1_000_000) : null, exchangeRateDate: form.valuationDate ? new Date(`${form.valuationDate}T12:00:00`).getTime() : null, valuationDate: form.valuationDate ? new Date(`${form.valuationDate}T12:00:00`).getTime() : null, includeInNetWorth: form.includeInNetWorth, status: form.status, notes: form.notes || null }); };
  const compatibleGoals = goals.filter((goal: any) => goal.status === "active" && goal.currency === form.currency.toUpperCase());
  return <DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>{investment ? "Editar posición" : "Nueva posición"}</DialogTitle><DialogDescription>La valuación es manual y no genera órdenes, transferencias ni precios de mercado.</DialogDescription></DialogHeader><form onSubmit={submit} className="grid gap-4 py-2"><div className="grid gap-2"><Label>Nombre de la posición</Label><Input required value={form.name} onChange={event => update("name", event.target.value)} placeholder="Ej. Fondo de emergencia o Bitcoin"/></div><div className="grid gap-4 sm:grid-cols-2"><FieldSelect label="Tipo" value={form.type} onChange={(value: string) => update("type", value)} options={investmentTypes}/><div className="grid gap-2"><Label>Institución, plataforma o emisor</Label><Input value={form.institution} onChange={event => update("institution", event.target.value)} placeholder="Opcional"/></div></div><div className="grid gap-4 sm:grid-cols-3"><FieldSelect label="Entidad" value={form.entityId} onChange={(value: string) => update("entityId", value)} options={[["none", "Sin entidad"], ...entities.map((item: any) => [String(item.id), item.name]) ]}/><FieldSelect label="Proyecto" value={form.projectId} onChange={(value: string) => update("projectId", value)} options={[["none", "Sin proyecto"], ...projects.filter((item: any) => form.entityId === "none" || item.entityId === Number(form.entityId)).map((item: any) => [String(item.id), item.name]) ]}/><FieldSelect label="Ámbito" value={form.scope} onChange={(value: string) => update("scope", value)} options={[["personal", "Personal"], ["business", "Empresa"], ["mixed", "Mixto"]]}/></div><div className="grid gap-4 sm:grid-cols-3"><div className="grid gap-2"><Label>Moneda</Label><Input required maxLength={3} value={form.currency} onChange={event => update("currency", event.target.value.toUpperCase())}/></div><MoneyInput label="Costo acumulado" value={form.cost} onChange={(value: string) => update("cost", value)}/><MoneyInput label="Valor manual actual" value={form.current} onChange={(value: string) => update("current", value)}/></div><div className="rounded-lg border border-primary/15 bg-primary/5 p-3"><FieldSelect label="Objetivo que respalda (opcional)" value={form.goalId} onChange={(value: string) => update("goalId", value)} options={[["none", "Sin objetivo vinculado"], ...compatibleGoals.map((goal: any) => [String(goal.id), goal.name]) ]}/><p className="mt-2 text-xs text-muted-foreground">El valor de esta posición se mostrará como capital vinculado al objetivo. No crea una aportación ni modifica patrimonio o el saldo manual de la meta.</p>{!compatibleGoals.length ? <p className="mt-1 text-xs text-amber-800">No hay objetivos activos en {form.currency.toUpperCase()} para vincular.</p> : null}</div>{form.currency !== reportCurrency ? <div className="rounded-lg border p-3"><p className="mb-3 text-sm font-medium">Conversión manual a {reportCurrency}</p><div className="grid gap-3 sm:grid-cols-2"><MoneyInput label={`Valor reportado (${reportCurrency})`} value={form.reportValue} onChange={(value: string) => update("reportValue", value)}/><div className="grid gap-2"><Label>Tipo de cambio usado</Label><Input inputMode="decimal" value={form.exchangeRate} onChange={event => update("exchangeRate", event.target.value)}/></div></div></div> : null}<div className="grid gap-4 sm:grid-cols-3"><div className="grid gap-2"><Label>Fecha de valuación</Label><Input type="date" value={form.valuationDate} onChange={event => update("valuationDate", event.target.value)}/></div><FieldSelect label="Estado" value={form.status} onChange={(value: string) => update("status", value)} options={[["active", "Activa"], ["paused", "En pausa"], ["closed", "Cerrada"]]}/><label className="flex items-center gap-2 pt-7 text-sm"><Checkbox checked={form.includeInNetWorth} onCheckedChange={value => update("includeInNetWorth", value === true)}/>Incluir en patrimonio</label></div><div className="grid gap-2"><Label>Notas privadas</Label><Textarea value={form.notes} onChange={event => update("notes", event.target.value)} maxLength={3000}/></div><Button type="submit" className="btn-primary" disabled={saving}>{saving ? "Guardando…" : "Guardar posición"}</Button></form></DialogContent>;
}

function FinancedAssetForm({ accounts, entities, projects, reportCurrency, saving, onSubmit }: any) {
  const [form, setForm] = useState({ entityId: "none", projectId: "none", sourceAccountId: "none", name: "", assetType: "vehicle", institution: "", scope: "personal", currency: reportCurrency, purchaseValue: "", cashContribution: "0", acquiredAt: toDateValue(new Date()), valuationPolicy: "depreciating", creditor: "", installment: "", installmentCount: "", interestRate: "", nextDueAt: "", endDate: "", notes: "" });
  const update = (key: string, value: string) => setForm(current => ({ ...current, [key]: value }));
  const money = (value: string) => Math.round((Number(value) || 0) * 100);
  const purchaseValueCents = money(form.purchaseValue);
  const cashContributionCents = money(form.cashContribution);
  const financedAmountCents = Math.max(0, purchaseValueCents - cashContributionCents);
  const sameCurrencyAccounts = accounts.filter((account: any) => account.status === "active" && account.currency === form.currency.toUpperCase());
  const submit = (event: FormEvent) => { event.preventDefault(); if (!purchaseValueCents || cashContributionCents >= purchaseValueCents) { toast.error("El valor de adquisición debe ser mayor que el anticipo para registrar una compra financiada."); return; } if (cashContributionCents > 0 && form.sourceAccountId === "none") { toast.error("Selecciona la cuenta desde la que pagaste el anticipo."); return; } onSubmit({ entityId: form.entityId === "none" ? null : Number(form.entityId), projectId: form.projectId === "none" ? null : Number(form.projectId), sourceAccountId: form.sourceAccountId === "none" ? null : Number(form.sourceAccountId), name: form.name, assetType: form.assetType, institution: form.institution || null, scope: form.scope, currency: form.currency.toUpperCase(), purchaseValueCents, cashContributionCents, acquiredAt: new Date(`${form.acquiredAt}T12:00:00`).getTime(), valuationPolicy: form.valuationPolicy, creditor: form.creditor || null, installmentCents: money(form.installment), installmentCount: form.installmentCount ? Number(form.installmentCount) : null, interestRateBps: form.interestRate ? Math.round(Number(form.interestRate) * 100) : null, nextDueAt: form.nextDueAt ? new Date(`${form.nextDueAt}T12:00:00`).getTime() : null, endDate: form.endDate ? new Date(`${form.endDate}T12:00:00`).getTime() : null, notes: form.notes || null }); };
  const projectOptions = projects.filter((project: any) => form.entityId === "none" || project.entityId === Number(form.entityId)).map((project: any) => [String(project.id), project.name]);
  return <DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-2xl"><DialogHeader><DialogTitle>Registrar activo financiado</DialogTitle><DialogDescription>Se crea un activo y una deuda vinculada. El anticipo queda registrado como traspaso patrimonial, no como gasto operativo. No se ejecuta ningún pago ni se cambia el saldo manual de tu cuenta.</DialogDescription></DialogHeader><form onSubmit={submit} className="grid gap-4 py-2"><div className="grid gap-4 sm:grid-cols-2"><div className="grid gap-2"><Label>Activo</Label><Input required value={form.name} onChange={event => update("name", event.target.value)} placeholder="Ej. Auto familiar o vivienda"/></div><FieldSelect label="Tipo" value={form.assetType} onChange={value => { update("assetType", value); if (value === "property") update("valuationPolicy", "appreciating"); if (value === "vehicle") update("valuationPolicy", "depreciating"); }} options={[["vehicle", "Vehículo"], ["property", "Vivienda o inmueble"], ["land", "Terreno"], ["other", "Otro activo"]]}/></div><div className="grid gap-4 sm:grid-cols-3"><FieldSelect label="Entidad" value={form.entityId} onChange={value => update("entityId", value)} options={[["none", "Sin entidad"], ...entities.map((item: any) => [String(item.id), item.name])]}/><FieldSelect label="Proyecto" value={form.projectId} onChange={value => update("projectId", value)} options={[["none", "Sin proyecto"], ...projectOptions]}/><FieldSelect label="Ámbito" value={form.scope} onChange={value => update("scope", value)} options={[["personal", "Personal"], ["business", "Empresa"], ["mixed", "Mixto"]]}/></div><div className="rounded-xl border border-primary/20 bg-primary/5 p-4"><p className="text-sm font-semibold">Valor y anticipo</p><div className="mt-3 grid gap-4 sm:grid-cols-3"><MoneyInput label={`Valor de adquisición (${form.currency})`} value={form.purchaseValue} onChange={value => update("purchaseValue", value)}/><MoneyInput label="Anticipo pagado" value={form.cashContribution} onChange={value => update("cashContribution", value)}/><div className="grid gap-2"><Label>Financiación calculada</Label><div className="flex h-10 items-center rounded-md border border-input bg-muted/40 px-3 text-sm font-semibold">{formatMoney(financedAmountCents, form.currency || reportCurrency)}</div></div></div><div className="mt-3 grid gap-4 sm:grid-cols-3"><div className="grid gap-2"><Label>Moneda</Label><Input required maxLength={3} value={form.currency} onChange={event => update("currency", event.target.value.toUpperCase())}/></div><div className="grid gap-2"><Label>Fecha de adquisición</Label><Input required type="date" value={form.acquiredAt} onChange={event => update("acquiredAt", event.target.value)}/></div><FieldSelect label="Política de valuación" value={form.valuationPolicy} onChange={value => update("valuationPolicy", value)} options={[["depreciating", "Pierde valor"], ["appreciating", "Gana valor"], ["manual", "Actualización manual"]]}/></div></div><div className="grid gap-4 sm:grid-cols-2"><FieldSelect label="Cuenta del anticipo" value={form.sourceAccountId} onChange={value => update("sourceAccountId", value)} options={[["none", "Sin anticipo desde cuenta"], ...sameCurrencyAccounts.map((account: any) => [String(account.id), `${account.name} · ${formatMoney(account.currentValueCents, account.currency)}`]) ]}/><div className="grid gap-2"><Label>Institución o vendedor</Label><Input value={form.institution} onChange={event => update("institution", event.target.value)} placeholder="Opcional"/></div></div>{cashContributionCents > 0 && form.sourceAccountId === "none" ? <p className="text-xs text-amber-700">Falta seleccionar la cuenta de origen del anticipo.</p> : null}<div className="rounded-xl border border-border/70 p-4"><p className="text-sm font-semibold">Financiación vinculada</p><div className="mt-3 grid gap-4 sm:grid-cols-3"><div className="grid gap-2"><Label>Acreedor</Label><Input value={form.creditor} onChange={event => update("creditor", event.target.value)} placeholder="Banco o financiera"/></div><MoneyInput label="Mensualidad prevista" value={form.installment} onChange={value => update("installment", value)}/><div className="grid gap-2"><Label>Número de mensualidades</Label><Input inputMode="numeric" min="1" type="number" value={form.installmentCount} onChange={event => update("installmentCount", event.target.value)} placeholder="Opcional"/></div></div><div className="mt-3 grid gap-4 sm:grid-cols-3"><div className="grid gap-2"><Label>Tasa anual (%)</Label><Input inputMode="decimal" min="0" step="0.01" value={form.interestRate} onChange={event => update("interestRate", event.target.value)} placeholder="Opcional"/></div><div className="grid gap-2"><Label>Próximo vencimiento</Label><Input type="date" value={form.nextDueAt} onChange={event => update("nextDueAt", event.target.value)}/></div><div className="grid gap-2"><Label>Fin previsto</Label><Input type="date" value={form.endDate} onChange={event => update("endDate", event.target.value)}/></div></div></div><div className="grid gap-2"><Label>Notas privadas</Label><Textarea value={form.notes} onChange={event => update("notes", event.target.value)} maxLength={3000}/></div><Button type="submit" className="btn-primary" disabled={saving || !form.name || purchaseValueCents <= 0}>{saving ? "Registrando…" : "Registrar activo y financiación"}</Button></form></DialogContent>;
}

function OperationForm({ investment, transactions, saving, onSubmit }: any) { const [form, setForm] = useState({ type: "contribution", amount: "", occurredAt: toDateValue(new Date()), linkedTransactionId: "none", notes: "" }); const submit = (event: FormEvent) => { event.preventDefault(); onSubmit({ investmentId: investment.id, type: form.type, amountCents: Math.round((Number(form.amount) || 0) * 100), currency: investment.currency, occurredAt: new Date(`${form.occurredAt}T12:00:00`).getTime(), linkedTransactionId: form.linkedTransactionId === "none" ? null : Number(form.linkedTransactionId), notes: form.notes || null }); }; return <DialogContent className="sm:max-w-xl"><DialogHeader><DialogTitle>Registrar operación</DialogTitle><DialogDescription>La aportación, retiro y rendimiento actualizan el costo y valor actual de la posición. Para una variación de mercado, edita la valuación manual.</DialogDescription></DialogHeader><form onSubmit={submit} className="grid gap-4 py-2"><FieldSelect label="Tipo" value={form.type} onChange={(value: string) => setForm({ ...form, type: value })} options={operationTypes}/><MoneyInput label={`Importe (${investment.currency})`} value={form.amount} onChange={(value: string) => setForm({ ...form, amount: value })}/><div className="grid gap-2"><Label>Fecha</Label><Input required type="date" value={form.occurredAt} onChange={event => setForm({ ...form, occurredAt: event.target.value })}/></div><FieldSelect label="Movimiento manual ya registrado (opcional)" value={form.linkedTransactionId} onChange={(value: string) => setForm({ ...form, linkedTransactionId: value })} options={[["none", "Sin enlace"], ...transactions.filter((item: any) => item.currency === investment.currency).map((item: any) => [String(item.id), `${new Date(item.occurredAt).toLocaleDateString("es-MX")} · ${formatMoney(item.amountCents, item.currency)} · ${item.notes || item.type}`]) ]}/><div className="grid gap-2"><Label>Notas</Label><Textarea value={form.notes} onChange={event => setForm({ ...form, notes: event.target.value })}/></div><Button type="submit" className="btn-primary" disabled={saving || !form.amount || Number(form.amount) <= 0}>{saving ? "Guardando…" : "Guardar operación"}</Button></form></DialogContent>; }

function FieldSelect({ label, value, onChange, options }: { label: string; value: string; onChange: (value: string) => void; options: readonly (readonly string[])[] }) { return <div className="grid gap-2"><Label>{label}</Label><Select value={value} onValueChange={onChange}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent>{options.map(([optionValue, optionLabel]) => <SelectItem key={optionValue} value={optionValue}>{optionLabel}</SelectItem>)}</SelectContent></Select></div>; }
function MoneyInput({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <div className="grid gap-2"><Label>{label}</Label><Input required inputMode="decimal" min="0" step="0.01" value={value} onChange={event => onChange(event.target.value)}/></div>; }
