import { AmortizationInsights } from "@/components/AmortizationInsights";
import { DialogContent, DialogDescription, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { trpc } from "@/lib/trpc";

const money = (value: number, currency: string) => new Intl.NumberFormat("es-MX", { style: "currency", currency, minimumFractionDigits: 2 }).format(value / 100);

export function FinancedAssetAmortizationDialog({ debt }: { debt: { id: number; name: string } }) {
  const { data, isLoading } = trpc.finance.debts.amortization.useQuery({ debtId: debt.id });
  return <DialogContent className="max-h-[92vh] max-w-5xl overflow-y-auto"><DialogHeader><DialogTitle>Amortización de {debt.name}</DialogTitle><DialogDescription>Resumen, histórico conciliado y escenario de referencia. Consultar o exportar no registra pagos ni intereses.</DialogDescription></DialogHeader>{isLoading || !data ? <p className="py-8 text-sm text-muted-foreground">Preparando tabla…</p> : <div className="space-y-5"><AmortizationInsights debt={data.debt} payments={data.payments} projection={data.projection} adjustments={data.adjustments}/><section><h3 className="mb-2 text-sm font-semibold">Capital e intereses pagados</h3><div className="overflow-x-auto rounded-xl border"><table className="w-full min-w-[620px] text-sm"><thead className="bg-muted/50 text-left text-xs text-muted-foreground"><tr><th className="p-2">Fecha</th><th className="p-2 text-right">Pago total</th><th className="p-2 text-right">Capital</th><th className="p-2 text-right">Interés ordinario</th><th className="p-2 text-right">Interés vencido</th><th className="p-2 text-right">Cargos</th></tr></thead><tbody>{data.payments.length ? data.payments.map((payment: any) => <tr className="border-t" key={payment.id}><td className="p-2">{new Date(payment.paidAt).toLocaleDateString("es-MX")}</td><td className="p-2 text-right">{money(payment.totalPaymentCents, data.debt.currency)}</td><td className="p-2 text-right">{money(payment.principalCents, data.debt.currency)}</td><td className="p-2 text-right">{money(payment.interestCents, data.debt.currency)}</td><td className="p-2 text-right">{money(payment.lateInterestCents, data.debt.currency)}</td><td className="p-2 text-right">{money(payment.feeCents, data.debt.currency)}</td></tr>) : <tr><td className="p-5 text-center text-muted-foreground" colSpan={6}>Aún no hay cuotas conciliadas. Registra una cuota desde Planificación cuando tengas el gasto real confirmado.</td></tr>}</tbody></table></div></section><section><div className="mb-2 flex flex-wrap items-end justify-between gap-2"><div><h3 className="text-sm font-semibold">Escenario de amortización</h3><p className="text-xs text-muted-foreground">Proyección basada en tasa y mensualidad de referencia; no anticipa atrasos ni cargos futuros.</p></div><strong className="text-sm">Interés proyectado: {money(data.projection.totalProjectedInterestCents, data.debt.currency)}</strong></div>{data.projection.warning ? <p className="mb-3 rounded-lg border border-amber-200 bg-amber-50 p-3 text-sm text-amber-950">{data.projection.warning}</p> : null}<div className="overflow-x-auto rounded-xl border"><table className="w-full min-w-[640px] text-sm"><thead className="bg-muted/50 text-left text-xs text-muted-foreground"><tr><th className="p-2">Cuota</th><th className="p-2 text-right">Saldo inicial</th><th className="p-2 text-right">Interés ordinario</th><th className="p-2 text-right">Pago de referencia</th><th className="p-2 text-right">Capital</th><th className="p-2 text-right">Saldo final</th></tr></thead><tbody>{data.projection.rows.length ? data.projection.rows.map((row: any) => <tr className="border-t" key={row.installment}><td className="p-2">{row.installment}</td><td className="p-2 text-right">{money(row.openingBalanceCents, data.debt.currency)}</td><td className="p-2 text-right">{money(row.regularInterestCents, data.debt.currency)}</td><td className="p-2 text-right">{money(row.plannedPaymentCents, data.debt.currency)}</td><td className="p-2 text-right">{money(row.principalCents, data.debt.currency)}</td><td className="p-2 text-right">{money(row.closingBalanceCents, data.debt.currency)}</td></tr>) : <tr><td className="p-5 text-center text-muted-foreground" colSpan={6}>Completa la tasa y mensualidad de referencia de la deuda para proyectar cuotas.</td></tr>}</tbody></table></div></section></div>}</DialogContent>;
}
